# Apps

This directory is reserved for product surfaces in the EstateOS target monorepo:

- `web/` → customer-facing Next.js application for discovery, investor workspace, visa, insurance, payments, and documents.
- `admin/` → broker, admin, and compliance portal.
- `mobile/` → future mobile surface when justified.
